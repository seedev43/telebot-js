const notOwner = "Command for owner only";
const botNotAdmin = "Bot not Admin";
const notAdmin = "Not Admin";
const notGroup = "Command is available in group only";

module.exports = { botNotAdmin, notOwner, notAdmin, notGroup };
